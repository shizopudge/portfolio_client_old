import 'package:flutter/material.dart';

class Pallete {
  static const primary = Color.fromRGBO(26, 20, 35, 1);
  static const attention = Color.fromRGBO(247, 23, 53, 1);
  static const second = Color.fromRGBO(255, 247, 248, 1);
  static const addition = Color.fromRGBO(204, 201, 220, 1);

  static const transparent = Colors.transparent;
  static const black = Colors.black;
  static const white = Colors.white;
  static const grey = Colors.grey;
}
